name = "nximkb-site"
compatibility_date = "2024-04-03"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Pages configuration
# The 'main' and 'assets' fields are not needed for Pages as they are handled automatically.
